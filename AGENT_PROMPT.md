# 일일 RWA/디지털자산 다이제스트 - 예약 에이전트 프롬프트

이 파일의 내용은 `schedule` 스킬로 등록하는 예약 에이전트(cloud routine)의
프롬프트로 그대로 사용한다. 매일 지정 시각에 아래 절차를 순서대로 실행할 것.

**중요**: Claude Code 클라우드 routine 샌드박스는 보안 정책상 임의 외부
도메인(뉴스 RSS 서버 등)에 대한 raw 네트워크 접근(Bash의 curl/python
requests 등)을 차단한다. 오직 **WebFetch 도구를 통한 접근만 허용**된다.
따라서 클라우드에서는 `collect.py`(feedparser 직접 호출)가 아니라
아래 "1. 수집" 절차의 WebFetch 기반 방식을 사용해야 한다. 로컬(PC)에서
수동 실행할 때는 네트워크 제약이 없으므로 `python src/collect.py`를 그냥
써도 된다.

---

## 절차 (클라우드 routine 기준)

1. **수집**: `config/sources.yaml`을 읽고, `url`이 비어있지 않은 각 소스에
   대해 다음을 반복한다:
   a. WebFetch(그 소스의 url, 아래 추출 프롬프트)를 호출한다:
      > "이 페이지는 RSS/Atom 피드입니다. 모든 항목(item/entry)을 빠짐없이
      > JSON 배열로 추출해줘. 각 원소는 정확히
      > {"title": "...", "url": "...", "published": "...", "summary": "..."}
      > 형식이어야 하고, summary는 피드에 있는 설명을 1~2문장으로 정리한
      > 것(없으면 빈 문자열). 다른 설명 텍스트 없이 JSON 배열만 출력해줘."
   b. WebFetch 결과(JSON 배열 텍스트)를 `data/_tmp_items.json` 파일에 그대로
      저장한다 (Write 도구 사용, 셸 이스케이프 문제 회피).
   c. 다음 명령으로 필터링/중복제거/누적 저장한다:
      ```
      python src/ingest_items.py --source "<소스 name>" --category <media|regulators|google_alerts> --lang <lang> --filter <filter 값> < data/_tmp_items.json
      ```
      (`<소스 name>`, `<category>`, `<lang>`, `<filter>`는 sources.yaml의
      해당 소스 필드 값을 그대로 사용)
   d. 특정 소스에서 WebFetch가 실패하거나 빈 결과를 반환해도 전체를 중단하지
      말고 다음 소스로 넘어간다. 실패한 소스는 나중에 요약 보고에 짧게
      언급한다.
   e. 모든 소스를 처리한 뒤 `data/digest_<YYYYMMDD>.json`을 읽는다
      (예: `data/digest_20260817.json`). 이 파일이 오늘의 누적 결과다.

2. JSON의 `items` 배열을 확인한다.

   - **`count`가 0인 경우**: 아래 "빈 다이제스트 메시지" 형식으로 짧은 상태
     메시지만 작성해 5번으로 건너뛴다. (봇이 정상 동작 중임을 알리는 용도)

   - **`count`가 1 이상인 경우**: 각 항목에 대해:
     - `summary` 필드가 비어 있거나 너무 짧으면(50자 미만), 해당 `url`을
       WebFetch로 다시 열어 기사 본문에서 핵심 내용을 파악한다. 기사 전체를
       가져오지 못하거나 유료 구독 장벽이 있으면 제목과 소스명만으로 판단한다.
     - 기사마다 한국어로 1~2문장 요약을 작성한다 (원문이 영어여도 한국어로).
     - 각 기사를 아래 5개 테마 중 하나로 분류한다:
       `규제` / `스테이블코인` / `토큰화 증권·펀드` / `기관·기업 동향` / `시장·가격`
       (애매하면 가장 가까운 하나만 선택, 중복 분류 금지)

3. 테마별로 기사를 그룹핑해 다이제스트 본문을 작성한 뒤, 마지막에
   **"오늘의 시사점"** 섹션을 추가한다:
   - 그날 수집된 기사들을 관통하는 흐름을 3~5개 불릿으로 종합한다.
   - 단순 사실 재나열이 아니라 "그래서 무엇을 의미하는가"를 쓴다. 예:
     규제 신호가 특정 산업군에 미칠 영향, 경쟁사 대비 포지셔닝 변화,
     스테이블코인 발행사 간 점유율 변화, 기관 자금 유입/이탈 신호 등.
   - implication이 뚜렷하지 않은 날은 억지로 만들지 말고 "특이 동향 없음,
     시장 정상 범위" 식으로 정직하게 쓴다.

4. 메시지를 Telegram `parse_mode=HTML` 규격으로 작성한다.
   - 허용 태그만 사용: `<b>` `<i>` `<u>` `<s>` `<a href="...">` `<code>` `<pre>`
   - `<ul>` `<li>` `<br>` 등은 지원되지 않는다. 목록은 `- ` 접두사 + 개행으로,
     줄바꿈은 실제 개행 문자로 표현한다.
   - 아래 "메시지 템플릿"을 따른다.
   - 완성된 텍스트를 `data/digest_send_<YYYYMMDD>.txt` 파일로 저장한다
     (예: `data/digest_send_20260817.txt`). 텔레그램 특수문자 이스케이프에
     신경 쓰지 않아도 되도록 파일에 그대로 쓰면 된다.

5. 발송한다:
   ```
   python src/send_telegram.py data/digest_send_<YYYYMMDD>.txt
   ```
   표준에러에 각 청크의 전송 상태(`status=200`)가 출력된다. 200이 아니거나
   오류가 발생하면 원인을 확인하고 (예: .env의 TELEGRAM_BOT_TOKEN/CHAT_ID
   누락) 사용자에게 알린다.

6. **중복방지 DB 영속화 (클라우드 routine에서 필수)**: 클라우드 routine은
   매 실행마다 저장소를 새로 clone하므로, `data/seen.sqlite3`를 커밋해두지
   않으면 다음 실행 때 오늘 상태가 사라져 같은 기사를 반복 발송하게 된다.
   발송 성공 여부와 무관하게(수집을 했다면) 항상 아래를 실행한다:
   ```
   git add data/seen.sqlite3
   git commit -m "Update seen store $(date -u +%Y-%m-%d)"
   git push
   ```
   커밋할 변경사항이 없으면(오늘 신규 기사가 0건) 조용히 건너뛴다.

7. 완료 후 요약 보고: 오늘 수집 건수, 실패한 소스(있다면), 발송 성공 여부,
   seen store 커밋 여부를 짧게 알린다. (긴 다이제스트 원문을 다시 출력할
   필요 없음 - 이미 텔레그램으로 전송됨)

---

## 메시지 템플릿

```
📰 <b>RWA/디지털자산 데일리 다이제스트 - 2026-08-17</b>
오늘 수집: N건

⚖️ <b>규제</b>
- <a href="url">기사제목</a> — 1~2문장 한국어 요약
- ...

💵 <b>스테이블코인</b>
- ...

🏛️ <b>토큰화 증권·펀드</b>
- ...

🏢 <b>기관·기업 동향</b>
- ...

📈 <b>시장·가격</b>
- ...

🔎 <b>오늘의 시사점</b>
- 시사점 1
- 시사점 2
- 시사점 3
```

해당 테마에 기사가 하나도 없으면 그 섹션 전체를 생략한다 (빈 헤더 출력 금지).

## 빈 다이제스트 메시지 (count == 0일 때)

```
📰 <b>RWA/디지털자산 데일리 다이제스트 - 2026-08-17</b>
오늘 새로 수집된 관련 뉴스가 없습니다. (정상 작동 중)
```

저장소의 README.md에 전체 설계 배경이 설명되어 있으니 참고할 것.
